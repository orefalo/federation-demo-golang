{
  "name": "federation-review",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "up": "npm i --no-package-lock && tsc && cd .. && faas rm ${npm_package_name} && faas up -f ${npm_package_name}.yml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/federation": "^0.10.2",
    "@snapcore/apollo-server-snapcore": "^1.0.1",
    "@graphql-modules/core": "^0.7.13",
    "graphql": "^14.5.8"
  },
  "devDependencies": {
    "@snapcore/snapcore-types": "^1.0.0",
    "@types/node": "^12.12.3",
    "typescript": "^3.6.4"
  }
}
